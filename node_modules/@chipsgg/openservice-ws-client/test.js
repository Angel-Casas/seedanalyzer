const WS = require("ws");
const Client = require("./dist/index");

const channels = ["public", "private", "auth"];

Client(WS, channels, console.log).then(({ state, actions }) => {
  // do somthing
});
